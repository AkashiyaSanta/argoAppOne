apiVersion: batch/v1
kind: Job
metadata:
  name: my-job2
spec:
  template:
    spec:
      containers:
      - name: my-container2
        image: busybox
        command: ["echo", "Hello, Kubernetes!"]
      restartPolicy: Never
  backoffLimit: 4
